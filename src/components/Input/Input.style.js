import { StyleSheet } from "react-native";
export default StyleSheet.create({
    input: {
        flex: 1,
        padding:0,
    },
    inputContainer: {
        borderWidth: 0.7,
        borderColor:'gray',
        borderRadius: 3,
        padding:8,
        alignItems:'center',
        flexDirection: 'row',
        marginVertical: 4,
    },
});