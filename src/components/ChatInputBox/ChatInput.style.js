import { StyleSheet } from "react-native";
export default StyleSheet.create({
    container: {
        borderWidth: 0.7,
        borderColor: 'black',
        margin: 8,
        padding: 4,
        borderRadius: 4,
        flexDirection:'row',
        backgroundColor:'white',
    },
    textInput: {
        flex:1,
    }
});